{
  "name": "hotel-erp-backend",
  "version": "2.0.0",
  "private": true,
  "description": "APEX Multi-Outlet Restaurant ERP - Python FastAPI Backend",
  "main": "dist/server.js",
  "bin": {
    "prisma": "./prisma-bridge.js"
  },
  "scripts": {
    "build": "python3 -m pip install -r requirements.txt || pip3 install --break-system-packages -r requirements.txt || pip install -r requirements.txt",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-4000}",
    "dev": "uvicorn app.main:app --reload --port ${PORT:-4000}",
    "prisma": "node ./prisma-bridge.js"
  }
}
